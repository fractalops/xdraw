use "xdraw/cards" as cards

diagram "Deployment topology" {
  gateway: cards.card "Gateway" {
    at (80, 120); size (220, 100)
  }
  api: cards.card "API" {
    at (420, 120); size (220, 100)
  }

  https: gateway@right -> api@left "HTTPS"
}
