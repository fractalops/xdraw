use "xdraw/architecture" as arch

diagram "Native frames" {
  workspace: frame "Workspace" {
    locked true

    request: arch.person "Request"
    service: arch.system "Service"
    request -> service

    data: frame "Data" {
      records: arch.database "Records"
    }

    service -> data.records
  }
}
