use "xdraw/architecture" as arch

diagram "Layered graph" {
  arrange layered { gap 28 }

  request: arch.person "Request"
  api: arch.system "API"
  worker: arch.system "Worker"
  records: arch.database "Records"

  request -> api
  api -> worker
  worker -> records

}
